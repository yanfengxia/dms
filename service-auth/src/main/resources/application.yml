

server:
  port: 55002



eureka:
  client:
    register-with-eureka: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: auth-service
  sleuth:
    enabled: true
    sampler:
      probability: 1
